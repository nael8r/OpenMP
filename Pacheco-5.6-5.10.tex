%				\begin{minted}[mathescape,linenos, numbersep=5pt, gobble=5, tabsize=3, frame=lines, framesep=2mm, fontsize=\tiny]{vhdl}
%					entity permutedChoice1 is
%						port(
%							-- Chave inicial
%							bus64In 	: IN std_logic_vector(0 TO 63);
%							-- Chave operada
%							bus56Out    : OUT std_logic_vector(0 TO 55)
%						);
%					end permutedChoice1;
%				\end{minted}

\section{Exemplos}
	\subsection{Ordenação}
		%5.6.1
		\subsubsection{Bubble Sort}
			\begin{frame}[fragile]
				\begin{minted}[mathescape,linenos, numbersep=5pt, gobble=5, tabsize=3, frame=lines, framesep=2mm, fontsize=\scriptsize]{c}
					for (list length = n; list length >= 2; list length  ) 
						for (i = 0; i < list length 1; i++)
							if (a[i] > a[i+1]) { 
								tmp = a[i];
                     			a[i] = a[i+1];
								a[i+1] = tmp;
							}
				\end{minted}
\end{frame}

			\begin{frame}
				RODOLFO
			\end{frame}
